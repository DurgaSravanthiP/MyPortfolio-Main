// Initialize typed.js for the `.text` element
const typed = new Typed('.text', {
    strings: ["Frontend Developer", "Web Developer", "ML Engineer"],
    typeSpeed: 100,
    backSpeed: 50,
    backDelay: 1000,
    loop: true
});